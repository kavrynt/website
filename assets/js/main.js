const menu=document.querySelector(".menu"),nav=document.querySelector(".nav");if(menu)menu.onclick=()=>nav.classList.toggle("open");
const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll(".reveal").forEach(e=>observer.observe(e));
const stages=[...document.querySelectorAll(".stage")],status=document.querySelector("#runStatus");if(stages.length){let n=0;function run(){stages.forEach(s=>s.classList.remove("processing","complete"));n=0;if(status)status.textContent="RUNNING";function next(){if(n)stages[n-1].classList.add("complete");if(n<stages.length){stages[n].classList.add("processing");n++;setTimeout(next,800)}else{stages.at(-1).classList.remove("processing");stages.at(-1).classList.add("complete");if(status)status.textContent="HEALTHY";setTimeout(run,2500)}}next()}run()}